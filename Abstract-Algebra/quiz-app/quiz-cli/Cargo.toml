[package]
name = "quiz-cli"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "quiz-cli"
path = "src/main.rs"

[dependencies]
quiz-core = { path = "../quiz-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
