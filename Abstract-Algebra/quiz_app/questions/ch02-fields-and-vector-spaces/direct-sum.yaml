choices:
- direct sum
- internal direct sum
difficulty: intermediate
explanation: The internal direct sum V = U ⊕ W requires every v ∈ V to decompose uniquely
  as u + w with u ∈ U, w ∈ W. Equivalently, the only vector in both is 0.
kind: blank
tags:
- vector-spaces
- direct-sum
text: A vector space V is the ___ of subspaces U and W if V = U + W and U ∩ W = {0}.
