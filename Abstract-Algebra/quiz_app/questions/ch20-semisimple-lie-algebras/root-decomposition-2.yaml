answer: 0
choices:
- g = h ⊕ ⊕_{α∈Φ} g_α where [H, X] = α(H)X for X ∈ g_α
- g = h ⊕ n⁺ ⊕ n⁻
- g = ⊕_{α∈Φ} g_α only
- g = h ⊕ ⊕_{α∈Φ} ℂE_α
difficulty: intermediate
explanation: 'The root decomposition g = h ⊕ ⊕_α g_α where g_α = {X ∈ g : [H,X] =
  α(H)X ∀H ∈ h} are the root spaces. Each root space g_α is 1-dimensional for simple
  g.'
kind: mc
tags:
- root-decomposition
- root-spaces
text: 'The root space decomposition of g with respect to h is:'
