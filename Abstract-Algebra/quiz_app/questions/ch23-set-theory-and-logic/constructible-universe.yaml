answer: 0
choices:
- L_0 = ∅, L_{α+1} = Def(L_α) (definable subsets), L = ∪_α L_α
- L = the universe of all sets
- L_α = P(L_{α-1}) (power set)
- L is the class of well-founded sets
difficulty: advanced
explanation: L_0=∅, L_{α+1}=Def(L_α) (first-order definable subsets of L_α), L_λ=∪_{α<λ}
  L_α for limits, L=∪_α L_α. In L, both AC and GCH hold. L is the minimal inner model
  of ZFC.
kind: mc
tags:
- constructible-universe
- inner-model
text: 'Gödel''s constructible universe L is defined by transfinite recursion as:'
