answer: 0
choices:
- P(0) holds, P(α) ⇒ P(α+1) (successor case), and [∀β<λ, P(β)] ⇒ P(λ) for limit ordinals
  λ
- P(0) holds and P(α) ⇒ P(α+1) only
- P holds for all finite ordinals
- P is definable in ZFC
difficulty: intermediate
explanation: 'Transfinite induction requires three cases: base (0), successor (α to
  α+1), and limit (λ = sup{β : β < λ}). The limit case is the crucial new ingredient
  vs ordinary induction.'
kind: mc
tags:
- ordinals
- transfinite-induction
text: 'Transfinite induction on ordinals states that a property P holds for all ordinals
  if:'
