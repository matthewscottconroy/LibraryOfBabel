answer: 0
choices:
- σ is true in all groups (i.e. σ is derivable from the group axioms)
- σ is true in some infinite group
- σ has no infinite models
- σ is undecidable
difficulty: intermediate
explanation: 'Compactness application: the set T = {group axioms} ∪ {∃x₁…xₙ(distinct)}
  has finite subsets with finite models. If σ fails for some infinite group G, compactness
  gives a finite counterexample (by adding ''size ≥ n'' axioms — contradiction).'
kind: mc
tags:
- compactness-theorem
- model-theory
text: 'Using the Compactness Theorem, one can prove that if σ is a sentence true in
  all finite groups, then:'
