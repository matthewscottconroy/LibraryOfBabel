# The Shape of a Loop

Three lessons.

`while` first, because it is the primitive one — everything else in this chapter
can be written with it, and understanding it as a Chapter 6 transition makes the
rest fall into place. Then `for`, which is an abbreviation that gathers a loop's
three moving parts into one line where they can be seen together. Then nesting,
where two loops meet and the cost multiplies.
