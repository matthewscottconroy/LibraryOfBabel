{
  "question_id": "11bb691e-cd4c-458f-9736-8a18b045f6dd",
  "chapter": 28,
  "phase": 6,
  "kind": "mc",
  "text": "Why does a `PreparedStatement` with a `?` placeholder prevent SQL injection where string concatenation does not?",
  "choices": [
    "Prepared statements are compiled once, and the compiled form cannot be modified afterwards.",
    "Concatenation puts user data into the query's grammar; a placeholder sends it separately as a value, so it cannot become syntax.",
    "The driver validates each parameter against the column's declared type and rejects mismatches.",
    "The placeholder escapes quote characters in the supplied value before substituting it."
  ],
  "answer": 1,
  "explanation": "Escaping is what a naive fix attempts and gets wrong; the placeholder never builds a query string at all. This is Chapter 24's distinction exactly — data in the grammar versus data as a value — and once you have written a parser the vulnerability is obvious in a way it was not before. It has been the top-ranked web vulnerability for most of two decades.",
  "tags": [
    "sql",
    "injection"
  ],
  "difficulty": "intermediate",
  "generated": true
}
