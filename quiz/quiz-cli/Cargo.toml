[package]
name = "quiz-cli"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "quiz-cli"
path = "src/main.rs"

[dependencies]
quiz-core = { path = "../quiz-core" }
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"
